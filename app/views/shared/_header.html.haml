#head
  %div
    .left
      %h2= link_to "cody krieger", root_path
      .desc= @desc.downcase
    .right.nav
      = link_to "home", root_path, :class => "#{active_page? '/', 'active'}"
      = link_to "apps & projects", "/projects", :class => "#{active_page? '/projects', 'active'}"
      = link_to "about", "/about", :class => "#{active_page? '/about', 'active'}"
      = link_to "blog", "/blog"
      = link_to "contact", "/contact", :class => "#{active_page? '/contact', 'active'}"
    .clear
